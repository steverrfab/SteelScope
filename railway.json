{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "node -v && npm -v && npm install && npx prisma generate && npx tsc --noEmit && npx next build --debug"
  },
  "deploy": {
    "startCommand": "npm run start",
    "preDeployCommand": "npx prisma db push && npm run seed:dev",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
